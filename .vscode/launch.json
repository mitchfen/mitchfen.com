{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch Edge",
            "type": "blazorwasm",
            "request": "launch",
            "cwd": "${workspaceFolder}/src",
            "browser": "edge",
        }
    ]
}   